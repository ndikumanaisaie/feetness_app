/* eslint-disable no-unused-vars */
import React, { useState } from 'react';
import { Box } from '@mui/material';

const Home = () => (
    <Box>
        <HeroBanner />
        <SearchExercises />
        <Exercises />
    </Box>
);

export default Home;